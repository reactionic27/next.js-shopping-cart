export type ChildernProps = {
  children: React.ReactNode;
};

export interface ProductsProps {
  id: number;
  title: string;
  price: number;
  category: string;
  description: string;
  image: string;
}
